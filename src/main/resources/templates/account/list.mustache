{{>layout/header}}

<!--마진 : mt, mr, ml, mb (1~5) ex) mt-5-->
<div class="container mt-2">
    <table class="table table-hover">
        <thead>
        <tr>
            <th>계좌번호</th>
            <th>잔액</th>
        </tr>
        </thead>
        <tbody>
        {{#models}}
            <tr>
                <th><a href="/account/1"> {{number}}</a></th>
                <th>{{balance}}원</th>
            </tr>
        {{/models}}
        </tbody>
    </table>
</div>

{{>layout/footer}}